export function ManageView() {
  return (
    <div className="wrap">
      <div className="section-head">
        <h2>Manage mode</h2>
      </div>
      <p className="mono" style={{ color: "var(--text-faint)" }}>
        Take over a squad, set the lineup, and re-run the simulation. Built in phase P4.
      </p>
    </div>
  );
}
